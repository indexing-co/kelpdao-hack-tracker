# For the dashboard, use the POOLED Neon endpoint (better for many short reads).
# The Indexing Co pipeline writes through the UNPOOLED endpoint — see the repo
# root .env.example for that.
DATABASE_URL=postgresql://user:password@host-pooler.neon.tech/dbname?sslmode=require&channel_binding=require
